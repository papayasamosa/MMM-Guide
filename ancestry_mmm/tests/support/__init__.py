"""Shared test-support builders (not test modules themselves).

See `lifecycle_fixture.py` for the PR 122 official curve-to-scenario
lifecycle fixture: a single, deterministic, already-fitted synthetic
project reused by both a plain pytest integration test and AppTest
coverage, so the fixture recipe exists in exactly one place.
"""
